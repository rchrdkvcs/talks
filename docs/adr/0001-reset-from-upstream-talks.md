# Reset from upstream talks

This repository started as a clone of Anthony Fu's Slidev talks repository, but it was reset into an independent personal presentation manager. The upstream talks and Git history were removed so the main branch contains only the new structure, conventions, and talks owned by this repository.
